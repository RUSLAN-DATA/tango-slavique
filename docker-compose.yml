services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: tango
      POSTGRES_PASSWORD: tango
      POSTGRES_DB: tango_slavique
    ports:
      - "5432:5432"
    volumes:
      - tango_pg:/var/lib/postgresql/data

volumes:
  tango_pg:
